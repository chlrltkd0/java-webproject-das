<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ChartDAO">

	<select id="getDeliveryVolume" parameterType="StartEndDateVO" resultMap="ChartResultSet">
		<![CDATA[
 			SELECT r.receiving_dt AS date, count(*) AS count FROM delivery AS d JOIN receiving AS r ON d.id=r.id 
 			WHERE r.receiving_dt >= #{startDate} AND r.receiving_dt <= #{endDate} GROUP BY r.receiving_dt ORDER BY r.receiving_dt;
 		]]> 
	</select>
	
	<resultMap type="DeliveryVolumeVO" id="ChartResultSet">
		<result property="date" column="date" />
		<result property="count" column="count" />
	</resultMap>

</mapper>
